---
title: "MDX Components Test"
date: "2026-04-23"
author: "Test Author"
readTime: "3 min"
category: "Testing"
excerpt: "A test post for MDX components."
---

# MDX Components Test

## Inline Code

Use `console.log()` to debug your code.

## Code Block

```typescript
function greet(name: string): string {
  return `Hello, ${name}!`
}

const result = greet("World")
console.log(result)
```

## Blockquote

> This is a blockquote. It should have a left border accent and italic styling.
>
> Multiple paragraphs in a blockquote.

## Table

| Name | Type | Description |
|---|---|---|
| `id` | `string` | Unique identifier |
| `name` | `string` | Display name |
| `active` | `boolean` | Whether enabled |

## Callout

<Callout type="info" title="Info">
This is an info callout with a title.
</Callout>

<Callout type="warning">
This is a warning callout without a title.
</Callout>

<Callout type="error" title="Error">
This is an error callout.
</Callout>
